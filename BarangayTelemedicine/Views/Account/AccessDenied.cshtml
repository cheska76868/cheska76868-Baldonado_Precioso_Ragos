@{
    Layout = null;
    ViewData["Title"] = "Access Denied";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Access Denied</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" />
    <style>
        body { background: #f0f4f8; display: flex; align-items: center; justify-content: center; min-height: 100vh; }
        .card { border: none; border-radius: 16px; box-shadow: 0 10px 40px rgba(0,0,0,0.1); max-width: 400px; text-align: center; }
    </style>
</head>
<body>
<div class="card p-5">
    <div style="font-size:4rem;">🚫</div>
    <h4 class="mt-3 fw-bold text-danger">Access Denied</h4>
    <p class="text-secondary">You do not have permission to view this page.</p>
    <a href="/Dashboard/Index" class="btn btn-primary mt-2">Go to Dashboard</a>
</div>
</body>
</html>
